import 'package:flutter/material.dart';

class AppBorders {
  // Radio de borde global de 8px
  static const double radiusInput = 8.0;

  static final BorderRadius defaultRadius = BorderRadius.circular(radiusInput);
}
